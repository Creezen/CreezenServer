<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jayce.vexis.business.dao.ArticleDao">
    <insert id="saveArticle" parameterType="articleBean"
        useGeneratedKeys="true" keyProperty="articleId">
        insert into article(userId, title, createTime, updateTime, favor)
        values (#{userId}, #{title}, #{createTime}, #{updateTime}, #{favor})
    </insert>
    
    <insert id="saveParagraph" parameterType="sectionBean">
        insert into section(articleId, content) values (#{articleId}, #{content})
    </insert>

    <select id="getArticle" resultType="articleBean">
        select * from article
    </select>
    
    <select id="getSections" parameterType="Long" resultType="sectionBean">
        select * from section where articleId = #{articleId} order by sectionId
    </select>

    <select id="getComment" parameterType="Long" resultType="remarkBean">
        select * from remark where sectionId = #{paragraphId}
    </select>

    <insert id="insertComment" parameterType="remarkBean">
        insert into remark(articleId, sectionId, userId, comment, favor, createTime)
            values (#{articleId}, #{paragraphId}, #{userId}, #{cotent}, #{favor}, #{createTime})
    </insert>
</mapper>