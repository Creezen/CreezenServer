<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc" xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/context
                           http://www.springframework.org/schema/context/spring-context.xsd
                           http://www.springframework.org/schema/mvc
                           http://www.springframework.org/schema/mvc/spring-mvc.xsd
                           http://www.springframework.org/schema/aop
                           https://www.springframework.org/schema/aop/spring-aop.xsd
                           http://www.springframework.org/schema/tx
                           http://www.springframework.org/schema/tx/spring-tx.xsd">

    <context:property-placeholder location="classpath:config.properties"/>

    <bean id="jedisPoolConfig" class="redis.clients.jedis.JedisPoolConfig">
        <property name="maxIdle" value="25"/>
        <property name="maxTotal" value="20"/>
        <property name="minIdle" value="0"/>
    </bean>

    <bean id="jedisConnectionFactory" class="org.springframework.data.redis.connection.jedis.JedisConnectionFactory">
        <property name="hostName" value="127.0.0.1"/>
        <property name="port" value="6379"/>
        <property name="database" value="0"/>
        <property name="timeout" value="1000"/>
        <property name="poolConfig" ref="jedisPoolConfig"/>
        <property name="password" value=""/>
    </bean>

    <bean id="stringRedisTemplate" class="org.springframework.data.redis.core.StringRedisTemplate">
        <property name="connectionFactory" ref="jedisConnectionFactory"/>
    </bean>
    <bean id="stringOpt" factory-bean="stringRedisTemplate" factory-method="opsForValue"/>
    <bean id="hashOpt" factory-bean="stringRedisTemplate" factory-method="opsForHash"/>
    <bean id="listOpt" factory-bean="stringRedisTemplate" factory-method="opsForList"/>
    <bean id="setOpt" factory-bean="stringRedisTemplate" factory-method="opsForSet"/>
    <bean id="zsetOpt" factory-bean="stringRedisTemplate" factory-method="opsForZSet"/>
    <bean id="streamOpt" factory-bean="stringRedisTemplate" factory-method="opsForStream"/>
    <bean id="geoOpt" factory-bean="stringRedisTemplate" factory-method="opsForGeo"/>
    <bean id="hyperLogOpt" factory-bean="stringRedisTemplate" factory-method="opsForHyperLogLog"/>
    <bean id="clusterOpt" factory-bean="stringRedisTemplate" factory-method="opsForCluster"/>

    <bean id="serverSocket" class="java.net.ServerSocket">
        <constructor-arg name="port" value="5786"/>
    </bean>

    <bean id="socketManger" class="com.jayce.vexis.foundation.socket.EventCenter">
        <constructor-arg name="serverSocket" ref="serverSocket"/>
    </bean>

    <bean id="redisAspect" class="com.jayce.vexis.foundation.RedisAspect"/>
    <aop:aspectj-autoproxy proxy-target-class="true"/>
    <aop:config>
        <aop:pointcut id="redisOperations"
                      expression="execution(* org.springframework.data.redis.core.RedisOperations+.*(..))
                        or execution(* org.springframework.data.redis.core.ValueOperations+.*(..))
                        or execution(* org.springframework.data.redis.core.HashOperations+.*(..))
                        or execution(* org.springframework.data.redis.core.ListOperations+.*(..))
                        or execution(* org.springframework.data.redis.core.SetOperations+.*(..))
                        or execution(* org.springframework.data.redis.core.ZSetOperations+.*(..))
                        or execution(* org.springframework.data.redis.core.StreamOperations+.*(..))
                        or execution(* org.springframework.data.redis.core.GeoOperations+.*(..))
                        or execution(* org.springframework.data.redis.core.HyperLogLogOperations+.*(..))
                        or execution(* org.springframework.data.redis.core.ClusterOperations+.*(..))"/>

        <aop:aspect ref="redisAspect">
            <aop:around method="intercept" pointcut-ref="redisOperations"/>
        </aop:aspect>
    </aop:config>

    <bean id="baseDruidDataSource" abstract="true" class="com.alibaba.druid.pool.DruidDataSource">
        <property name="driverClassName" value="${JDBC.driver}"/>

        <property name="maxActive" value="${JDBC.maxActive}"/>
        <property name="minIdle" value="${JDBC.minIdle}"/>
        <property name="initialSize" value="${JDBC.initialSize}"/>
        <property name="maxWait" value="${JDBC.maxWait}"/>
        <property name="testWhileIdle" value="${JDBC.testWhileIdle}"/>
        <property name="validationQuery" value="${JDBC.validationQuery}"/>
        <property name="timeBetweenEvictionRunsMillis" value="${JDBC.timeBetweenEvictionRunsMillis}"/>
        <property name="removeAbandoned" value="${JDBC.removeAbandoned}"/>
        <property name="removeAbandonedTimeout" value="${JDBC.removeAbandonedTimeout}"/>

        <property name="filters" value="${JDBC.filters}"/>
    </bean>

    <beans profile="LOCAL">
        <bean id="druidDataSource" parent="baseDruidDataSource">
            <property name="url" value="${JDBC.localUrl}"/>
            <property name="username" value="${JDBC.username}"/>
            <property name="password" value="${JDBC.password}"/>
        </bean>
    </beans>

    <beans profile="CLOUD">
        <bean id="druidDataSource" parent="baseDruidDataSource">
            <property name="url" value="${JDBC.cloudUrl}"/>
            <property name="username" value="${JDBC.username}"/>
            <property name="password" value="${JDBC.password}"/>
        </bean>
    </beans>

    <beans>
        <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
            <property name="dataSource" ref="druidDataSource"/>
            <property name="configLocation" value="classpath:Mybatis/MybatisConfig.xml"/>
        </bean>

        <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
            <property name="basePackage" value="com.jayce.vexis.business.dao"/>
            <property name="sqlSessionFactoryBeanName" value="sqlSessionFactory"/>
        </bean>

        <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
            <property name="dataSource" ref="druidDataSource"/>
        </bean>
        <tx:annotation-driven/>
    </beans>
</beans>